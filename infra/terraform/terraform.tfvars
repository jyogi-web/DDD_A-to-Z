# このファイルは terraform apply 時に terraform.tfvars として読み込まれる。
# NOTE: 今回は機密事項が入っていないため、terraform.tfvars に直接書いているが、機密事項を入れる場合は terraform.tfvars を .gitignore に追加して、terraform.tfvars.example を作成してそちらに記載すること。
project_id  = "tech-lang-war"
github_repo = "jyogi-web/DDD_A-to-Z"
